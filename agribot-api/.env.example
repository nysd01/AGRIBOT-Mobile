# Copy to .env. SQLite for local dev; Postgres in docker-compose / k8s.
DATABASE_URL=sqlite:///./agribot.db
# Host ports (change any that are already taken on your host)
API_PORT=18080
PROM_PORT=19090
GRAFANA_PORT=13000
# DATABASE_URL=postgresql://agribot:agribot@db:5432/agribot
