global:
  scrape_interval: 15s

scrape_configs:
  - job_name: "agribot-api"
    metrics_path: /metrics
    static_configs:
      - targets: ["api:8000"]   # inside docker-compose network; use host:port on a VPS
