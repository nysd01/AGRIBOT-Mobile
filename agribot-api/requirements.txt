# AGRIBOT Sensor API — cloud backend (deployable, testable, containerizable)
fastapi>=0.110
uvicorn[standard]>=0.29
sqlalchemy>=2.0
pydantic>=2.6
pydantic-settings>=2.2
psycopg2-binary>=2.9                    # PostgreSQL driver (production)
prometheus-fastapi-instrumentator>=7.0  # exposes /metrics for Prometheus

# ── test/dev only ──
pytest>=8.0
pytest-cov>=5.0
httpx>=0.27                             # required by FastAPI TestClient
